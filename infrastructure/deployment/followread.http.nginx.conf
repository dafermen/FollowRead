# Temporary NEW site used only to obtain FollowRead's certificate.
server {
    listen 80;
    listen [::]:80;
    server_name followread.innovalogic.tech;
    server_tokens off;
    location /.well-known/acme-challenge/ { root /var/www/followread-acme; }
    location / { return 503; }
}
